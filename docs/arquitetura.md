# 🏗️ Arquitetura do Sistema

Este documento descreve a arquitetura geral do Atlas, seus componentes e fluxos de dados.

---

## 📋 Índice

1. [Visão Geral](#visão-geral)
2. [Diagrama de Arquitetura](#diagrama-de-arquitetura)
3. [Componentes do Sistema](#componentes-do-sistema)
4. [Modelo de Dados](#modelo-de-dados)
5. [Fluxos de Autenticação](#fluxos-de-autenticação)
6. [Fluxo de Requisições](#fluxo-de-requisições)
7. [Estrutura Multi-tenant](#estrutura-multi-tenant)

---

## 🎯 Visão Geral

O Atlas é uma aplicação web moderna seguindo arquitetura **SPA (Single Page Application)** com backend API RESTful:

- **Frontend:** React SPA que consome APIs REST
- **Backend:** Django REST Framework fornecendo APIs stateless
- **Autenticação:** JWT (JSON Web Tokens) via SimpleJWT
- **Banco de Dados:** PostgreSQL com relacionamentos complexos
- **Cache:** Redis (preparado para filas Celery futuras)

### Princípios Arquiteturais

| Princípio | Implementação |
|-----------|---------------|
| **Separação de Responsabilidades** | Frontend e Backend completamente desacoplados |
| **API-First** | Backend expõe APIs RESTful consumíveis por qualquer cliente |
| **Stateless** | Servidor não mantém estado de sessão (JWT) |
| **Multi-tenant** | Dados segregados por empresa/departamento |
| **RBAC** | Controle de acesso baseado em cargos (Django Groups) |

---

## 📊 Diagrama de Arquitetura

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                  CLIENTE                                     │
│                          (Browser / Mobile App)                              │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             FRONTEND (React)                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │   Pages     │  │ Components  │  │  Contexts   │  │     Services        │ │
│  │             │  │             │  │             │  │                     │ │
│  │ • Dashboard │  │ • Header    │  │ • Auth      │  │ • api.js (axios)    │ │
│  │ • Titulares │  │ • Sidebar   │  │ • Permission│  │ • titulares.js      │ │
│  │ • Empresas  │  │ • Forms     │  │ • System    │  │ • empresas.js       │ │
│  │ • Pesquisa  │  │ • Guards    │  │             │  │ • auth.js           │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│                                      │                                       │
│                           JWT Token Storage                                  │
│                          (localStorage/memory)                               │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │ HTTP/REST
                                      │ + JWT Header
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           BACKEND (Django DRF)                               │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                         Middleware Layer                              │   │
│  │  • CORS • JWT Auth • Request Parsing • Exception Handling            │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                      │                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │   Views     │  │ Serializers │  │ Permissions │  │    URL Routing      │ │
│  │ (ViewSets)  │  │             │  │             │  │                     │ │
│  │             │  │ • Titular   │  │ • IsAuth    │  │ /api/v1/titulares/  │ │
│  │ • Titular   │  │ • Empresa   │  │ • CargoBase │  │ /api/v1/empresas/   │ │
│  │ • Empresa   │  │ • User      │  │             │  │ /api/auth/login/    │ │
│  │ • User      │  │ • Vinculo   │  │             │  │                     │ │
│  └──────┬──────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│         │                                                                    │
│  ┌──────▼──────────────────────────────────────────────────────────────┐    │
│  │                          Models (ORM)                                │    │
│  │  accounts: User, Sistema, Departamento, UsuarioVinculo              │    │
│  │  titulares: Titular, Dependente, VinculoTitular, VinculoDependente  │    │
│  │  empresa: Empresa                                                    │    │
│  │  core: Amparo, TipoAtualizacao                                      │    │
│  └──────┬──────────────────────────────────────────────────────────────┘    │
│         │                                                                    │
└─────────┼────────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           DATA LAYER                                         │
│                                                                              │
│   ┌─────────────────────────┐      ┌─────────────────────────┐              │
│   │      PostgreSQL 16      │      │        Redis 7          │              │
│   │                         │      │                         │              │
│   │  • Dados principais     │      │  • Cache de sessão      │              │
│   │  • Histórico (audit)    │      │  • Filas Celery (futuro)│              │
│   │  • Permissões           │      │  • Rate limiting        │              │
│   │                         │      │                         │              │
│   └─────────────────────────┘      └─────────────────────────┘              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Componentes do Sistema

### Backend - Django Apps

```
backend/apps/
├── accounts/          # Gestão de identidade e acesso
│   ├── models.py      # User, Sistema, Departamento, UsuarioVinculo
│   ├── views.py       # Auth views (Login, Logout, Refresh)
│   ├── viewsets.py    # ViewSets CRUD de usuários
│   ├── permissions.py # CargoBasedPermission
│   └── backends.py    # Backend de autenticação customizado
│
├── titulares/         # Domínio principal - Gestão de Titulares
│   ├── models.py      # Titular, Dependente, Vinculo*
│   ├── views.py       # ViewSets com permissões
│   └── serializers.py # Serialização de dados
│
├── empresa/           # Gestão de Empresas
│   ├── models.py      # Empresa
│   └── views.py       # EmpresaViewSet
│
└── core/              # Tabelas auxiliares
    └── models.py      # Amparo, TipoAtualizacao
```

### Frontend - Estrutura React

```
frontend/src/
├── components/        # Componentes reutilizáveis
│   ├── Header/        # Barra superior com usuário
│   ├── Sidebar/       # Menu lateral
│   ├── PermissionGuard/ # Controle de acesso visual
│   └── common/        # Botões, inputs, modais
│
├── context/           # Estado global (Context API)
│   ├── AuthContext.jsx      # Autenticação e usuário
│   ├── PermissionContext.jsx # Permissões RBAC
│   └── SystemContext.jsx    # Sistema/Depto selecionado
│
├── hooks/             # Custom hooks
│   └── useModelPermissions.js # Verificação de permissões
│
├── pages/             # Páginas/Rotas
│   ├── Dashboard/
│   ├── TitularList/TitularForm/
│   ├── DependenteList/DependenteForm/
│   ├── EmpresaList/EmpresaForm/
│   ├── Pesquisa/
│   └── Login/
│
├── services/          # Camada de API
│   ├── api.js         # Axios instance + interceptors
│   ├── auth.js        # Endpoints de autenticação
│   ├── titulares.js   # CRUD titulares
│   └── empresas.js    # CRUD empresas
│
└── utils/             # Utilitários
    ├── formatters.js  # Formatação de dados
    └── validators.js  # Validações
```

---

## 🗄️ Modelo de Dados

### Diagrama ER Simplificado

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              ACCOUNTS APP                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│     Sistema     │     │  Departamento   │     │      User       │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ id              │     │ id              │     │ id              │
│ nome            │     │ nome            │     │ email (unique)  │
│ slug            │     │ slug            │     │ nome            │
│ descricao       │     │ descricao       │     │ tipo_usuario    │
│ ativo           │     │ ativo           │     │ empresa_id (FK) │
└────────┬────────┘     └────────┬────────┘     │ is_active       │
         │                       │              │ groups (M2M)    │ ──► Cargo
         │      ┌────────────────┘              └────────┬────────┘
         │      │                                        │
         ▼      ▼                                        ▼
    ┌────────────────────┐                    ┌──────────────────────┐
    │  UsuarioVinculo    │◄───────────────────│    auth_group        │
    ├────────────────────┤                    │       (Cargo)        │
    │ id                 │                    ├──────────────────────┤
    │ usuario_id (FK)    │                    │ id                   │
    │ sistema_id (FK)    │                    │ name                 │
    │ departamento_id(FK)│                    │ permissions (M2M)    │
    │ ativo              │                    └──────────────────────┘
    └────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                             TITULARES APP                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│    Titular      │     │  VinculoTitular │     │   Dependente    │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ id              │     │ id              │     │ id              │
│ nome            │     │ titular_id (FK) │     │ titular_id (FK) │
│ nome_social     │     │ empresa_id (FK) │     │ nome            │
│ cpf             │     │ consulado_id    │     │ cpf             │
│ rnm             │     │ amparo_id (FK)  │     │ rnm             │
│ passaporte      │     │ ativo           │     │ passaporte      │
│ nacionalidade   │     │ data_inicio     │     │ parentesco      │
│ data_nascimento │     │ data_fim        │     │ data_nascimento │
│ email           │     └─────────────────┘     └─────────────────┘
│ foto            │
└─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              EMPRESA APP                                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────┐
│        Empresa          │
├─────────────────────────┤
│ id                      │
│ razao_social            │
│ nome_fantasia           │
│ cnpj (unique)           │
│ inscricao_estadual      │
│ inscricao_municipal     │
│ email                   │
│ telefone                │
│ endereco, cidade, uf... │
│ status                  │
│ ativo                   │
└─────────────────────────┘
```

### Relacionamentos Principais

| Origem | Destino | Tipo | Descrição |
|--------|---------|------|-----------|
| User | Empresa | N:1 | Usuário pertence a uma empresa |
| User | Group (Cargo) | N:N | Usuário tem um ou mais cargos |
| UsuarioVinculo | User | N:1 | Vincula usuário a sistema/depto |
| UsuarioVinculo | Sistema | N:1 | Sistema disponível para usuário |
| UsuarioVinculo | Departamento | N:1 | Departamento do usuário |
| Titular | VinculoTitular | 1:N | Titular tem múltiplos vínculos |
| Titular | Dependente | 1:N | Titular tem dependentes |
| VinculoTitular | Empresa | N:1 | Vínculo com empresa |
| VinculoTitular | Amparo | N:1 | Tipo de amparo legal |

---

## 🔐 Fluxos de Autenticação

### Login Flow

```
┌────────┐         ┌────────┐         ┌────────┐         ┌────────┐
│ Client │         │Frontend│         │Backend │         │Database│
└───┬────┘         └───┬────┘         └───┬────┘         └───┬────┘
    │                  │                  │                  │
    │  1. Login Form   │                  │                  │
    │─────────────────►│                  │                  │
    │                  │                  │                  │
    │                  │ 2. POST /api/auth/login/            │
    │                  │  {email, password}                  │
    │                  │─────────────────►│                  │
    │                  │                  │                  │
    │                  │                  │ 3. Validate User │
    │                  │                  │─────────────────►│
    │                  │                  │◄─────────────────│
    │                  │                  │                  │
    │                  │ 4. Response      │                  │
    │                  │  {access, refresh, user}            │
    │                  │◄─────────────────│                  │
    │                  │                  │                  │
    │                  │ 5. Store tokens  │                  │
    │                  │   (localStorage) │                  │
    │                  │                  │                  │
    │ 6. Redirect to   │                  │                  │
    │    Dashboard     │                  │                  │
    │◄─────────────────│                  │                  │
```

### Token Refresh Flow

```
┌────────┐         ┌────────┐         ┌────────┐
│Frontend│         │ Axios  │         │Backend │
└───┬────┘         └───┬────┘         └───┬────┘
    │                  │                  │
    │ API Request      │                  │
    │─────────────────►│                  │
    │                  │                  │
    │                  │ Request + JWT    │
    │                  │─────────────────►│
    │                  │                  │
    │                  │ 401 Unauthorized │
    │                  │◄─────────────────│
    │                  │                  │
    │                  │ POST /api/auth/refresh/
    │                  │  {refresh}       │
    │                  │─────────────────►│
    │                  │                  │
    │                  │ {access: new}    │
    │                  │◄─────────────────│
    │                  │                  │
    │                  │ Retry original   │
    │                  │ request + new JWT│
    │                  │─────────────────►│
    │                  │                  │
    │                  │ 200 OK + data    │
    │                  │◄─────────────────│
    │                  │                  │
    │ Response         │                  │
    │◄─────────────────│                  │
```

---

## 📡 Fluxo de Requisições

### Requisição API Típica

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            REQUEST FLOW                                      │
└─────────────────────────────────────────────────────────────────────────────┘

1. Frontend                    2. Axios Interceptor           3. Network
   ┌─────────────┐               ┌─────────────┐               ┌─────────────┐
   │ Component   │──────────────►│ Add JWT     │──────────────►│ HTTP POST   │
   │ calls API   │               │ Header      │               │ /api/v1/... │
   └─────────────┘               └─────────────┘               └─────────────┘
                                                                      │
                                                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            BACKEND PROCESSING                                │
└─────────────────────────────────────────────────────────────────────────────┘

   4. Django Middleware         5. DRF Auth                   6. Permission Check
   ┌─────────────┐               ┌─────────────┐               ┌─────────────┐
   │ CORS        │──────────────►│ JWTAuth     │──────────────►│ CargoBase   │
   │ Parsing     │               │ Verify JWT  │               │ Permission  │
   └─────────────┘               └─────────────┘               └─────────────┘
                                                                      │
                                                                      ▼
   7. ViewSet Action            8. Serializer                 9. Database
   ┌─────────────┐               ┌─────────────┐               ┌─────────────┐
   │ list/create │──────────────►│ Validate    │──────────────►│ ORM Query   │
   │ retrieve... │               │ Serialize   │               │ PostgreSQL  │
   └─────────────┘               └─────────────┘               └─────────────┘
                                                                      │
                                                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            RESPONSE FLOW                                     │
└─────────────────────────────────────────────────────────────────────────────┘

   10. JSON Response            11. Axios Interceptor         12. Component
   ┌─────────────┐               ┌─────────────┐               ┌─────────────┐
   │ HTTP 200    │◄──────────────│ Handle      │◄──────────────│ Update      │
   │ + Data      │               │ Errors      │               │ State       │
   └─────────────┘               └─────────────┘               └─────────────┘
```

---

## 🏢 Estrutura Multi-tenant

O Atlas suporta múltiplos cenários de isolamento de dados:

### Modelo de Segregação

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         EMPRESA (Tenant Principal)                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │ Empresa A                                                                ││
│  │  ├── Usuários (tipo_usuario=INTERNO)                                    ││
│  │  │    ├── João (Consultor, Consular)                                    ││
│  │  │    ├── Maria (Gestor, Jurídico)                                      ││
│  │  │    └── Carlos (Diretor, todos departamentos)                         ││
│  │  │                                                                       ││
│  │  └── Titulares (via VinculoTitular)                                     ││
│  │       ├── Titular 1 → Vínculo com Empresa A                             ││
│  │       └── Titular 2 → Vínculo com Empresa A                             ││
│  └─────────────────────────────────────────────────────────────────────────┘│
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │ Empresa B                                                                ││
│  │  ├── Usuários (tipo_usuario=CLIENTE)                                    ││
│  │  │    └── Pedro (acesso limitado aos seus titulares)                    ││
│  │  │                                                                       ││
│  │  └── Titulares (via VinculoTitular)                                     ││
│  │       └── Titular 3 → Vínculo com Empresa B                             ││
│  └─────────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
```

### Níveis de Acesso

| Nível | Tipo Usuário | Escopo | Descrição |
|-------|--------------|--------|-----------|
| **Admin** | INTERNO | Global | Superusuário, acesso total |
| **Diretor** | INTERNO | Multi-depto | Acesso a todos departamentos |
| **Gestor** | INTERNO | Departamento | Gerencia seu departamento |
| **Consultor** | INTERNO | Departamento | Visualização no departamento |
| **Cliente** | CLIENTE | Empresa | Acesso aos dados da sua empresa |

---

## 🔗 Próxima Leitura

- [Backend](backend.md) - Detalhes de implementação Django
- [Frontend](frontend.md) - Detalhes de implementação React
- [Permissões](permissoes.md) - Sistema RBAC completo
